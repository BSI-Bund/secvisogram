# Secvisogram Dependabot configuration file

version: 2
updates:
  - package-ecosystem: 'npm'
    directory: '/app'
    schedule:
      interval: 'weekly'
    # Disable version updates for this repository
    open-pull-requests-limit: 5
    allow:
      - dependency-type: 'production'
