{
  "name": "secvisogram",
  "private": true,
  "scripts": {
    "dev": "cd app && npm run dev",
    "build": "cd app && npm run build",
    "test": "cd app && npm test",
    "postinstall": "npm run -s install:app",
    "install:app": "cd app && npm ci"
  },
  "repository": {
    "type": "git",
    "url": "git@gitlab-ext.exxcellent.de:bsi/secvisogram.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^2.2.1",
    "typescript": "^4.1.5"
  }
}
