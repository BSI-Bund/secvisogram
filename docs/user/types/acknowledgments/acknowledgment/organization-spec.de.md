# Organization - Spezifikation

Die beitragende Organisation (`organization`) hat den Werttyp `string` mit 1 oder mehr Zeichen und enthält den Namen der beitragenden Organisation, die anerkannt wird.

*Beispiele:*

* `CISA`
* `Google Project Zero`
* `Talos`
